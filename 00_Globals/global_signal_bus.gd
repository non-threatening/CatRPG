extends Node
@warning_ignore_start("unused_signal")

signal desaturate( thing )

## Lock pick mini game; state_freq.gd, freq_lock.gd
signal frequency_match( frequency, wave_pos, threshold_time, slop, up_or_side )
signal frequency_matched()
signal freq_tut_show_stick( freq_tut_triggers )
signal freq_hold( hold )
